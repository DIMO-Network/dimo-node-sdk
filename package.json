{
  "name": "dimo-node-sdk",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/developer-sdk",
    "packages/transactions"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "pack": "npm pack --workspaces",
    "publish-packages": "npm publish --workspace=packages/transactions && npm publish --workspace=packages/developer-sdk"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "ts-jest": "^27.1.5",
    "ts-node": "^10.9.1",
    "tsc-alias": "^1.8.10",
    "tslib": "^2.6.0",
    "typescript": "^5.1.6"
  }
}
